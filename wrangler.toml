# Cloudflare Pages 설정
name = "jp-times"
compatibility_date = "2024-01-01"

[site]
# Next.js 정적 내보내기(output: 'export') 결과물 폴더
# 빌드 명령에서 반드시 npm run build 를 실행해야 이 폴더가 생성됩니다.
pages_build_output_dir = "out"
